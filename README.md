# qqqaaaa

This repository includes a tiny CLI tool named `tyar`.

## Usage

Run directly (no install):

```sh
node bin/tyar.js --help
node bin/tyar.js echo hello world
```

Or run tests:

```sh
npm test
```
